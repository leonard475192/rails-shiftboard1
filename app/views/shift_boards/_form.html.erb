<%= form_with(model: shift_board, local: true) do |form| %>
  <% if shift_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shift_board.errors.count, "error") %> prohibited this shift_board from being saved:</h2>

      <ul>
        <% shift_board.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :store_id %>
    <%= form.number_field :store_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
